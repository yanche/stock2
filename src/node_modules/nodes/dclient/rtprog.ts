
import * as cm from './common';
import * as d from '../def';

const getAll = cm.genGetAll<d.Rtprog>('rtprog');
const getMul = cm.genGetMul<d.Rtprog>('rtprog');
const get = cm.genGetOne<d.Rtprog>('rtprog');
const create = cm.genCreate<d.Rtprog>('rtprog');
const createMul = cm.genCreateMul<d.Rtprog>('rtprog', 2000);
const remove = cm.genRemove('rtprog');
const bulkUpdate = cm.genBulkUpdate('rtprog', false, 2000);
const bulkUpsert = cm.genBulkUpdate('rtprog', true, 2000);
const upsert = cm.genUpsert('rtprog');

export {get, getMul, getAll, create, createMul, remove, upsert, bulkUpdate, bulkUpsert}

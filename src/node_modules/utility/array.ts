
export function findFirst<T>(arr: Array<T>, comparer: (t: T) => boolean, fallback:T): T {
    var len = arr.length;
    for (var i = 0; i < len; ++i) {
        var v = arr[i];
        if (comparer(v)) return v;
    }
    return fallback;
}
